# Default style

# Font size
figure.titlesize : 18
axes.titlesize : 16
axes.labelsize : 14
xtick.labelsize : 12
ytick.labelsize : 12
legend.fontsize : 10
font.size : 10

# Figure appearance
figure.constrained_layout.use : True
figure.facecolor : w
axes.facecolor : w

# Figure output
savefig.dpi : 600
savefig.format : png

